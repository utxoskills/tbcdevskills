# Code Reference

This plugin keeps the canonical full code reference in:

- `tbc-dev/code-reference.md`

This wrapper exists so the plugin has a product-style `docs/` structure like larger skill libraries while preserving backward compatibility.

Use the canonical file above for:

- runnable TypeScript examples
- contract-specific construction flows
- broadcast ordering
- utility usage patterns
